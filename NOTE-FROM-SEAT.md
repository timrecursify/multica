# NOTE FROM SEAT 08:33Z
Your PROPOSED-DELETE predicate is wrong for this layout: a task dir (<ws>/<8hex>/) contains only workdir/AGENTS.md, codex-home/, logs/, multica-config/, .gc_meta.json, .managed_env.json. There is NO git checkout in it; repo clones live in /var/lib/gsp/multica/workspaces/.repos/<ws>/ (shared, never delete). So the only correct predicate is the DB: agent_task_queue row with id prefix = <8hex>, status in (completed,failed,cancelled), completed_at < now()-1h, and no non-terminal row for the same issue currently referencing that dir. Also read .gc_meta.json (it is the cleaner descriptor). Re-emit PROPOSED-DELETE.paths on that predicate (skip anything whose task row is missing: list those separately). Include per-dir bytes and a total. Then finish the terminal-transition fix.

ADDENDUM 08:36Z: .gc_meta.json already carries task_id/issue_id/completed_at, so the cleaner descriptor exists; find why the timer reclaims nothing (its ownership gate) and fix that rather than adding a new predicate. Most task dirs are ~23 MB (codex-home); the bulk sits in a few dirs whose workdir holds a full checkout — see the seat list appended below.

SEAT LIST 08:37Z (largest task dirs, bytes): da3c5c5c/2309f170 5019377664, da3c5c5c/1386c57f 5013831680, f47e92d1/4e799b64 2810413056, da3c5c5c/af35ccb6 1131741184, da3c5c5c/007c107a 1131446272. The checkout is at <task>/workdir/<repo>/ (e.g. workdir/ppp, ~5 GB each incl. node_modules) — run your pushed-HEAD check THERE, not at workdir/. PPP build tasks are the growth driver; GSP tasks are ~23 MB.
