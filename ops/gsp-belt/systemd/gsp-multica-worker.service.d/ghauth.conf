[Service]
# gh cannot use a git credential helper, so the belt reaches it through a shim
# on PATH that mints the same single-repository App token.
Environment=PATH=/usr/local/lib/gsp-belt/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
