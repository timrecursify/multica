[Service]
# The wrapper defaults its lock to /var/lib/gsp/.local/state, which ProtectSystem=strict
# leaves read-only. Keep it inside an existing ReadWritePaths entry.
Environment=MULTICA_DAEMON_LOCK_FILE=/var/lib/gsp-multica/state/gsp-multica-worker.lock
