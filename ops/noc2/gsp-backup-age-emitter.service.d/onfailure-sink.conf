[Unit]
# Route unit failure to the alert sink. The pre-existing noc2-alert@ chain is
# a no-op: NOC2_ALERTS_ENABLED=0 makes noc2-alert.sh log and exit 0, and its
# sender ~/bin/intercom-remote does not exist on this box.
OnFailure=gsp-unit-alert@%n.service
