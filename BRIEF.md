# Lane belt-rec6-rollup-deps-20260907 — P0: model rollups and waiting as durable dependencies

Astra recommendation 6 (`~/dev/belt-architecture-20260907/RESULT.md`). This is the belt's single largest source of wasted work RIGHT NOW.

## Measured, 2026-09-07 14:15Z

Relay journal, last 10 minutes: `rollup_has_open_children` x78, `invalid_transition` x15, `evidence_required` x8. Three MEGA parents produce nearly all of it — 2391, 2528, 24200 — each sitting in Registered with live children. The relay selects the parent, refuses it, and repeats. Those refusals consume the advance pass, whose duration already exceeds its 15-second cadence, so this directly throttles every other ticket.

17 parents currently have open children and a non-terminal status.

## Observed basis — re-read each

- Leaf-only execution is INTENTIONAL: `ops/belt/reconciler.cjs:13`, `ops/belt/sql/2026-09-03_leaf_dispatch.sql:13`. Never disable the leaf trigger to make the loop disappear.
- The bridge returns a structured 202 with retry metadata: `ops/belt/multica-bridge.cjs:2637`
- The generic advance failure branch marks non-409 failures as failed: `ops/belt/parity/multica-relay-advance-daemon.cjs:1221`

## The job

1. **A parent with open children must never be selected for advance and must never get a paid builder just to discover it has children.** Exclude it at the selection query, not by refusing it after selection. That single change should end the 78-per-10-minutes churn.
2. **Persist the dependency.** Store blocked-on child IDs plus a dependency version / next-eligibility timestamp. Consume child state changes to WAKE the parent — completion or cancellation of the last child triggers exactly one aggregation. A parent with no wakeup owner is a liveness defect; that is the actual bug.
3. **Distinguish response classes:** accepted-deferred, refused, and failed. Honor the returned retry condition. A legitimate 202 dependency wait must NOT be logged as a failed relay run and regenerated by another loop.
4. **Define mixed child outcomes explicitly** (some Done, some Cancelled, some failed) and add cycle detection.
5. **Verify against live data:** after the fix, `rollup_has_open_children` must stop appearing in the relay journal for parents that are correctly excluded. Watch 2 minutes and report the count.

## Coordination — do not collide

- ALPHA-000358 owns the concrete stuck-ticket data classes and `invalid_transition` enumeration. You own the ROLLUP DEPENDENCY MODEL in code.
- ALPHA-000371 (Astra) is bundling tickets into MEGA leaves as a data operation. Your code change must make its output dispatchable, not fight it.
- ALPHA-000373 owns the advance/gate runners (~lines 940-1260) of the relay daemon. Your selection-query change is at `:1085-1122`; keep your diff to the selection and dependency logic and rebase if you collide.
- Do NOT touch `build-admission.cjs`, `qc-strict-evidence.cjs` or `multica-bridge.cjs` verdict ingestion (ALPHA-000365 owns those).

## Scope — write

`ops/belt/reconciler.cjs`, the selection/dependency logic in `ops/belt/parity/multica-relay-advance-daemon.cjs`, open-child admission in `ops/belt/multica-bridge.cjs` (admission only, NOT verdict ingestion), a migration under `ops/belt/sql/` with paired rollback if dependency state needs storage, plus relay/reconciler tests.

## Constraints

- PR against timrecursify/multica, branch from origin/main. Self-merge when green. Do NOT deploy, restart, or apply a migration — the seat owns that.
- Never touch PPP-23686. Never rotate creds. Never print secrets. No Multica ticket.
- Production DB is READ ONLY: `sudo -n /bin/bash -c "docker exec gsp-multica-v2-postgres-1 psql -U gsp_multica -d gsp_multica -At -c \"...\""`
Sandbox: no rm, no multi-line bash, one sleep <=120s. Observed vs inferred, labelled; cite path:line.
RESULT.md: Outcome first line, the wakeup design, before/after `rollup_has_open_children` counts, the PR.
